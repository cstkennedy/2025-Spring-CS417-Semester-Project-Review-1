"""
This module is a collection of least squares approximation functions for the
CPU Temperatures Project.  All code may be used freely in the semester project,
iff it is imported using ``import least_squares`` or
``from least_squares import {...}`` where ``{...}`` represents one or more
functions.
"""


def linear(times: list[float], core_temps: list[float]):
    """
    T.B.W.

    Returns:
        one tuple in the form...

        (start time, end time, y-intercept, slope)
    """

    raise NotImplementedError()
